apiVersion: v1
kind: Pod
metadata:
  name: resources
spec:
  containers:
    - name: nginx
      image: nginx
      resources:
        requests: #Soft limit
          cpu: "100m" #1000m =1CPU
          memory: "128Mi"
        limits: #Hard limit
          cpu: "150m"
          memory: "256Mi"
