apiVersion: v1
kind: Service
metadata:
  name: nginxnodeservice
  labels:
    project: roboshop
    environment: dev
spec:
  type: NodePort
  selector:
    project: roboshop
    tier: frontend
    component: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007

---
apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    project: roboshop
    tier: frontend
    component: frontend
spec:
  containers:
    - name: nginx
      image: nginx
